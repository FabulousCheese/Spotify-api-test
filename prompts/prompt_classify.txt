你是一个资深接口自动化测试架构师。请对以下 17 个 Spotify API 端点进行分类，
判断每个端点适合「YAML 数据驱动」还是「需要代码编排」。


## Albums
  - GET /albums/{id} | operationId: get-an-album | summary: Get Album
  - GET /albums | operationId: get-multiple-albums | summary: Get Several Albums [DEPRECATED]
  - GET /albums/{id}/tracks | operationId: get-an-albums-tracks | summary: Get Album Tracks
  - GET /artists/{id}/albums | operationId: get-an-artists-albums | summary: Get Artist's Albums
  - GET /me/albums | operationId: get-users-saved-albums | summary: Get User's Saved Albums
  - PUT /me/albums | operationId: save-albums-user | summary: Save Albums for Current User [DEPRECATED]
  - DELETE /me/albums | operationId: remove-albums-user | summary: Remove Users' Saved Albums [DEPRECATED]
  - GET /me/albums/contains | operationId: check-users-saved-albums | summary: Check User's Saved Albums [DEPRECATED]
  - GET /browse/new-releases | operationId: get-new-releases | summary: Get New Releases [DEPRECATED]

## Artists
  - GET /artists/{id} | operationId: get-an-artist | summary: Get Artist
  - GET /artists | operationId: get-multiple-artists | summary: Get Several Artists [DEPRECATED]
  - GET /artists/{id}/top-tracks | operationId: get-an-artists-top-tracks | summary: Get Artist's Top Tracks [DEPRECATED]
  - GET /artists/{id}/related-artists | operationId: get-an-artists-related-artists | summary: Get Artist's Related Artists [DEPRECATED]
  - GET /me/following | operationId: get-followed | summary: Get Followed Artists
  - PUT /me/following | operationId: follow-artists-users | summary: Follow Artists or Users [DEPRECATED]
  - DELETE /me/following | operationId: unfollow-artists-users | summary: Unfollow Artists or Users [DEPRECATED]
  - GET /me/following/contains | operationId: check-current-user-follows | summary: Check If User Follows Artists or Users [DEPRECATED]


## 分类标准

### data_driven（YAML 驱动）
满足以下所有条件：
1. HTTP GET 方法（只读操作）
2. 不修改服务端状态（无副作用）
3. 不需要前置 setup（不依赖其他端点的创建操作）
4. 不需要后置 cleanup（不用撤销/恢复数据）
5. 单次请求即可验证，不涉及多步链路

### need_code（需要代码编排）
满足以下任一条件：
1. HTTP POST/PUT/PATCH/DELETE 方法（有写操作）
2. 会修改服务端状态（save/remove/follow/unfollow/add/delete 等）
3. 测试需要 setup 前置 → action → verify → teardown 链路
4. 与其他端点存在前后依赖关系（如: 先创建资源再校验再删除）
5. 需要动态生成测试数据（参数值依赖前一步返回结果）

## 输出要求

只输出一个 JSON 对象，不要有任何解释文字：

```json
{
  "data_driven": [
    {
      "tag": "Albums",
      "method": "GET",
      "path": "/albums/{id}",
      "operation_id": "get-an-album",
      "reason": "只读查询，无副作用，单次请求即可"
    }
  ],
  "need_code": [
    {
      "tag": "Albums",
      "method": "PUT",
      "path": "/me/albums",
      "operation_id": "save-albums-user",
      "reason": "PUT 写操作，修改用户状态，需要 cleanup 恢复"
    }
  ]
}
```

注意：
- reason 字段用中文简述分类理由，15字以内
- 每个端点都必须在两个数组之一中出现
- 只输出 JSON，不要 Markdown 标记
